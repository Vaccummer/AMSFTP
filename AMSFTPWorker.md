# AMWorkManager
AMWokerManager is a plus version of AMSFTPWorker, it will implement abilities listed below
1. wk.transfer is a new independent version, user can input addtional dedicated interupt flag and transfer_call instead of sharing the class object. Remeber that user only pass essential parameters, some attr in pd can init in function. class shared ProgressData,TransferCallback will be deprecated
2. wk has a work threading, task queue, result queue(or list)，this thread is instristic, end until wk destruct. when task queue empty, sleep. esle fectch task task and output result
3. wk will add a no-blocking version of tranfer function, same parameter but return task ID immediately. ID can be generated by [inline uint64_t GenerateUID()] . the task is added to task queque and wait for conducting
4. add a task_info class, including attr: ID submit_time, start_time, status[pending, conducting, finished(enum)], finished_time, rcm, 
current_file, current_filesize_transferred, current_file_total_size, total_transferred_size, total_size, TASKS, interrupt_flag, timeout_ms, start_time(for timeout calculate)  
ps1: time(except timeout steady clock), using [inline double timenow()]to get   
ps2: you have to check pending task is interrtupted or timeout, if is, put them to result instead of waiting for conduct
4. add staus query function, recive id, return task status
4. add result query function, recive id, return std::optional\<task_info\>  
ps:  
1. you can adjsut some variable names if neccessary